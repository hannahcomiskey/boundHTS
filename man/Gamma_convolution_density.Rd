% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Gamma_convolution_density.R
\name{Gamma_convolution_density}
\alias{Gamma_convolution_density}
\title{Monte Carlo Convolution Density for Gamma Distributions}
\usage{
Gamma_convolution_density(
  z,
  shape_matrix,
  rate_matrix,
  bottom_samps,
  n_sims = 100
)
}
\arguments{
\item{z}{Numeric evaluation point for the aggregated density.}

\item{shape_matrix}{Matrix of shape parameters for the Gamma
distribution for each observation (rows correspond to draws, columns correspond to nodes).}

\item{rate_matrix}{Matrix of rate parameters for the Gamma
distribution for each observation (rows correspond to draws, columns correspond to nodes).}

\item{bottom_samps}{Array of bottom series samples for each element contributing to the
aggregate (dimensions: n_sims x n_draws x b).}

\item{n_sims}{Number of simulations to generate per draw. Defaults to 100.}
}
\value{
A numeric value representing the estimated aggregated density evaluated at \code{z}.
}
\description{
Monte Carlo Convolution Density for Gamma Distributions
}
\details{
This function computes a Monte Carlo approximation of the density of an
aggregated random variable formed from a sum of Gamma-distributed
components. For each Monte Carlo simulation and posterior draw, the density
is evaluated using \code{stats::rgamma()}, and the result is averaged
across simulations and posterior draws.
}
\examples{
set.seed(1)

# Simulation setup
n_sims <- 50
n_draws <- 10
b <- 2

# Simulated samples
bottom_samps <- array(rgamma(n_sims * n_draws * b),
                        dim = c(n_sims, n_draws, b))

shape_matrix <- matrix(runif(n_draws * b, 2, 5), nrow = n_draws)
rate_matrix  <- matrix(runif(n_draws * b, 2, 5), nrow = n_draws)

weights <- c(1, 1)

Gamma_convolution_density(
  z = 0.5,
  shape_matrix = shape_matrix,
  rate_matrix = rate_matrix,
  bottom_samps = bottom_samps,
  n_sims = 100
)

}
