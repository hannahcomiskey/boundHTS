% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Poisson_convolution_density.R,
%   R/Poisson_convolution_density_point.R
\name{Poisson_convolution_density}
\alias{Poisson_convolution_density}
\title{Monte Carlo Convolution Density for Poisson}
\usage{
Poisson_convolution_density(x, lambda_vector)

Poisson_convolution_density(x, lambda_vector)
}
\arguments{
\item{x}{Numeric evaluation point for the aggregated density.}

\item{lambda_matrix}{Matrix of lambda parameters for the Poisson
distribution for each bottom level observation (rows correspond to simulations).}

\item{lambda_point}{Vector of lambda parameters for the Poisson
distribution for each bottom level observation.}
}
\value{
A numeric value representing the estimated aggregated density evaluated at \code{z}.

A numeric value representing the estimated aggregated density evaluated at \code{z}.
}
\description{
Monte Carlo Convolution Density for Poisson

Convolution Density for Poisson
}
\details{
This function computes a Monte Carlo approximation of the density of an
aggregated random variable formed from a sum of Poisson-distributed
components. For each Monte Carlo simulation and posterior draw, the density
is evaluated using \code{stats::dpois()}, and the result is averaged
across simulations.

This function computes an approximation of the density of an
aggregated random variable formed from a sum of Poisson-distributed
components. The density is evaluated using \code{stats::dpois()}.
}
\examples{
set.seed(1)

# Simulation setup
n_sims <- 50
n_bottom <- 5

# Simulated lambda samples
lambda_matrix <- matrix(rnorm(n_sims * n_bottom), nrow = n_sims)

Poisson_convolution_density(x = 5, lambda_matrix = lambda_matrix)
set.seed(1)

# Simulation setup
n_sims <- 50
n_bottom <- 5

# Simulated lambda samples
lambda_vector <- rnorm(n_bottom)

Poisson_convolution_density(x = 5, lambda_vector = lambda_vector)
}
